package no.mnemonic.services.grafeo.service.ti.delegates;

/**
 * Marker interface used by {@link DelegateProvider} to enforce type-safety when injecting delegates.
 * Must be implemented by all delegates.
 */
interface Delegate {
}
